$schema: https://moonrepo.dev/schemas/project.json
id: "test"
type: unknown

tasks:
  make-phony-file:
    command: bash
    args:
      - -c
      - 'echo "This is a phony file" > $workspaceRoot/target/test/phony-file.txt'
    outputs:
      - /target/test/
